/**
 * Template Rendering Utilities
 *
 * Main exports for the template rendering module
 */

const { TemplateRenderer } = require('./template-renderer');

module.exports = {
  TemplateRenderer
};

// Default export for convenience
module.exports.default = TemplateRenderer;
